import React from "react"
import Layout from "../components/layout"

const NotFoundPage = () => (
  <Layout>
    <main className="contenedor-principal">
      <h1>Error</h1>
      <p>La página a la cual quieres ingresar no existe</p>
    </main>
  </Layout>
)

export default NotFoundPage
